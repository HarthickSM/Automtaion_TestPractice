<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<!-- Runs the smoke pack in two browsers in parallel. Requires the browsers to be installed
     locally, or run.mode=remote with a Selenium Grid. -->
<suite name="Cross Browser Suite" verbose="1" parallel="tests" thread-count="2">

    <listeners>
        <listener class-name="com.tap.framework.listeners.TestListener"/>
        <listener class-name="com.tap.framework.listeners.RetryTransformer"/>
    </listeners>

    <test name="Chrome">
        <parameter name="browser" value="chrome"/>
        <groups>
            <run>
                <include name="smoke"/>
            </run>
        </groups>
        <packages>
            <package name="com.tap.tests"/>
        </packages>
    </test>

    <test name="Firefox">
        <parameter name="browser" value="firefox"/>
        <groups>
            <run>
                <include name="smoke"/>
            </run>
        </groups>
        <packages>
            <package name="com.tap.tests"/>
        </packages>
    </test>
</suite>
